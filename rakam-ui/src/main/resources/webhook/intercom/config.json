{
  "name": "Intercom.io",
  "description": "Collects data from Intercom",
  "parameters": {
    "collection_prefix": {
      "type": "string",
      "placeholder": "intercom",
      "description": "The collection prefix of Intercom events, for example conversation events will be collected as intercom_conversation if the prefix is 'intercom'"
    },
    "app_id": {
      "type": "string",
      "placeholder": "pyjppnbq",
      "description": "Intercom app id"
    },
    "hub_secret": {
      "type": "string",
      "placeholder": "",
      "description": "Hub secret that you entered in Intercom"
    }
  }
}